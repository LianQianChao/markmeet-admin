<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.meet.markmeetadmin.mapper.UserMapper">

    <select id="selectUserByNameAndPassword" parameterType="LoginVO" resultType="int">
        select count(1)
        from user
        where username = #{username}
          and password = #{password}
    </select>

    <select id="getAllUsers" resultType="User">
        select *
        from user
    </select>

    <insert id="addUser" parameterType="UserVO">
        insert into user(id, username, password, nickname, email)
        values (id, #{username}, #{password}, #{nickname}, #{email})
    </insert>
</mapper>
